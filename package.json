{
  "name": "cc-testador-subagents",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Infraestrutura de testes do plugin cc-testador-subagents. lib/ e scripts/ usam apenas builtins do Node; a unica camada com dependencia de runtime (Playwright + axe-core) vive isolada em runner/.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "fast-check": "4.9.0"
  }
}
